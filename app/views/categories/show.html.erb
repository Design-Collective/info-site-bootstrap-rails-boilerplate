<% content_for :meta_title do %><%= @category.name %><% end %>
<% content_for :title do %><%= @category.name %><% end %>

<h2>Pages that have <%= @category.name %> as category</h2>
<ul>
  <% @category.pages.each do |page| %>
    <li><%= page.title %></li>
  <% end %>
</ul>